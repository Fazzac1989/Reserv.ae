node_modules
dist
# The Expo web export. `dist` alone does not match it, so a local build made
# `format:check` fail on generated bundles that CI never sees.
dist-web
.next
.turbo
coverage
pnpm-lock.yaml
packages/db/src/generated

# The build plan is a source document, not code. Leave its formatting alone.
reservai-mvp-build-plan.md
